---
description: gd-news fork 的工作边界
alwaysApply: true
---

# 红线（必须先停下来问我）

- 修改 `.github/workflows/`、`scripts/update_news.py`、`data/*.json` 的 schema。
- 删除任何 `data/`、`feeds/`、`docs/` 下已有文件。
- 提交内容包含：`feeds/follow.opml`、`.env`、token、cookie、API key、个人邮箱/订阅源。

# 改前必读

- `README.md`：项目当前能做什么。
- `AGENTS.md`：工作纪律。
- `skills/ai-news-radar/SKILL.md`：数据流与源策略。

# 改后必跑

- `.venv/bin/python -m py_compile scripts/update_news.py`
- `.venv/bin/python -m pytest -q`

# GitHub 协作方式

- 用户是 GitHub 小白。涉及 GitHub、GitHub Actions、Pages、fork、branch、commit、push、PR、workflow permissions 等操作时，优先由 Agent 使用可用工具自动完成。
- 如果某个 GitHub 操作必须用户在网页上手动完成，Agent 要一步步说明点击哪里、看哪个页面元素、预期会出现什么结果。
- 不要只给抽象概念或链接；要把命令、页面路径和下一步验证方式说清楚。
